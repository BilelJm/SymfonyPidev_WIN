{% extends 'back-officeprogramme.html.twig' %}

{% block title %}OptionTransport{% endblock %}

{% block body %}
    <h1>OptionTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_transport.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ option_transport.matricule }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ option_transport.capacite }}</td>
            </tr>
        </tbody>
    </table>

    <a class= "btn btn-success" href="{{ path('option_transport_index') }}">Retourner a la liste</a>

    <a href="{{ path('option_transport_edit', {'id': option_transport.id}) }}">edit</a>

    {{ include('option_transport/_delete_form.html.twig') }}
{% endblock %}
